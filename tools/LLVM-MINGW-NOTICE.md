# LLVM-MinGW toolchain notice

Nexus pins the upstream LLVM-MinGW archive identified in `tools/llvm-mingw.lock`.

The verified archive contains `LICENSE.TXT`, which states that the LLVM project is distributed under the Apache License 2.0 with LLVM Exceptions and includes the applicable notices for the bundled components.

The archive itself is downloaded from the upstream `mstorsjo/llvm-mingw` GitHub release and is not committed into the Nexus Git history.
